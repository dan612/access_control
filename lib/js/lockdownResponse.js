/**
 * @file
 * Provides admin utilities.
 */

(function ($, Drupal) {

    'use strict';

    // Drupal.behaviors.accessControl = {
    //     attach: function (context, settings) {
    //       $('.espn-headlines-preview .headline', context).click(function () {
    //         $(this) -- send data to external analytics on click
    //       });
    //     }
    //   };

})(jQuery, Drupal);
